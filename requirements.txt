# ---------------------------------------------------------
# Core requirements for the finalized esri-cli toolkit.
# Ensure all packages are installed for full CLI functionality.
# ---------------------------------------------------------

# Core Framework & CLI Engine
typer>=0.9.0
rich>=13.0.0
python-dotenv>=0.19.0

# Data Processing & GIS Analytics
pandas
arcgis>=2.0.0